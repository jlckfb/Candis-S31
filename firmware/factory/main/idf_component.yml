dependencies:
  idf: ">=5.3"
  espressif/usb_host_msc: "^1.1.4"
  espressif/esp_asrc: "~1.1"
  # The TE observer API is used by factory_display.c; declare the local
  # esp_lvgl_port explicitly (it was previously pulled in transitively
  # through the BSP's pre-adapter override).
  espressif/esp_lvgl_port:
    version: "^2.9.0"
    override_path: ../../../../../espressif-Prj/esp-bsp/components/esp_lvgl_port
